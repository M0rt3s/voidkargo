<div class="nav-head">
    <a class="vk-brand" href="">
        <span class="vk-brand__void">void</span><span class="vk-brand__kargo">kargo</span><span class="vk-brand__mark">.</span>
    </a>
</div>

<input type="checkbox" title="Navigation menu" class="nav-toggler" />

<div class="nav-scrollable" onclick="document.querySelector('.nav-toggler').click()">
    <nav class="nav-group">
        <span class="vk-label vk-label--dim nav-group__title">Operations</span>

        <NavLink class="nav-item" href="" Match="NavLinkMatch.All">
            <span class="nav-item__label">Overview</span>
        </NavLink>

        <NavLink class="nav-item" href="counter">
            <span class="nav-item__label">Counter</span>
        </NavLink>

        <NavLink class="nav-item" href="weather">
            <span class="nav-item__label">Weather</span>
        </NavLink>
    </nav>

    <nav class="nav-group">
        <span class="vk-label vk-label--dim nav-group__title">Account</span>

        <AuthorizeView>
            <Authorized>
                <NavLink class="nav-item" href="profile">
                    <span class="nav-item__label">Profile (@context.User.FindFirst("display_name")?.Value)</span>
                </NavLink>

                <form method="post" action="/account/logout">
                    <AntiforgeryToken />
                    <button class="nav-item nav-item--button" type="submit">
                        <span class="nav-item__label">Log out</span>
                    </button>
                </form>
            </Authorized>
            <NotAuthorized>
                <NavLink class="nav-item" href="login">
                    <span class="nav-item__label">Log in</span>
                </NavLink>

                <NavLink class="nav-item" href="register">
                    <span class="nav-item__label">Register</span>
                </NavLink>
            </NotAuthorized>
        </AuthorizeView>
    </nav>
</div>
